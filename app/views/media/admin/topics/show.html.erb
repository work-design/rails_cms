<div class="ui segment breadcrumb">
  <%= link_to 'Back', admin_topics_path, class: 'section' %>
  <div class="divider">/</div>
  <div class="active section">Show</div>
</div>

<table class="ui very basic large table">
  <tbody>
    <tr>
      <td class="right aligned"><%= Topic.human_attribute_name(:title) %></td>
      <td><%= @topic.title %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= Topic.human_attribute_name(:author) %></td>
      <td><%= @topic.author %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= Topic.human_attribute_name(:tag_strings) %></td>
      <td><%= @topic.tag_strings %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= Topic.human_attribute_name(:body) %></td>
      <td><%= @topic.body %></td>
    </tr>
  </tbody>
</table>
